{
  "name": "la-scimmia-vince-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "icons": "node scripts/icons.mjs",
    "dev": "astro dev",
    "build": "npm run icons && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --experimental-strip-types --no-warnings scripts/check-simulator.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "sharp": "^0.34.0",
    "typescript": "^6.0.3"
  }
}
